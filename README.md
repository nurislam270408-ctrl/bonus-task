## Bonus Task: Dijkstra's Algorithm

### Implementation Details
- **Edge Weight Support**: Modified the `Edge` class to include a `weight` field. Updated the `Graph` structure to handle weighted edges using an Adjacency List.
- **Algorithm**: Implemented `void dijkstra(int start)` using standard arrays for tracking distances and visited nodes, utilizing simple loops instead of a `PriorityQueue` as permitted by the instructions.
- **Output**: The results are formatted and printed clearly in the console, showing the shortest distance from the starting vertex to all other vertices.
- ![Result](./screenshot/2026-05-23_00-18-08.png)